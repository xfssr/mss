# Руководство: Как вносить и сохранять изменения в репозитории Git

## 📚 Оглавление
1. [Базовый рабочий процесс](#базовый-рабочий-процесс)
2. [Команды Git шаг за шагом](#команды-git-шаг-за-шагом)
3. [Практические примеры](#практические-примеры)
4. [Работа с ветками](#работа-с-ветками)
5. [Часто используемые команды](#часто-используемые-команды)

---

## Базовый рабочий процесс

```
1. Изменить файлы
   ↓
2. Проверить изменения (git status)
   ↓
3. Добавить в staging (git add)
   ↓
4. Зафиксировать изменения (git commit)
   ↓
5. Отправить на сервер (git push)
```

---

## Команды Git шаг за шагом

### Шаг 1: Проверить статус репозитория

```bash
git status
```

Эта команда покажет:
- Какие файлы изменены
- Какие файлы готовы к коммиту (в staging)
- Какие файлы не отслеживаются Git

**Пример вывода:**
```
On branch copilot/debug-site-issues
Changes not staged for commit:
  modified:   package.json
  modified:   lib/mappers.ts

Untracked files:
  SETUP.md
```

### Шаг 2: Добавить файлы в staging area

**Добавить все изменённые файлы:**
```bash
git add .
```

**Добавить конкретный файл:**
```bash
git add package.json
```

**Добавить несколько файлов:**
```bash
git add package.json lib/mappers.ts SETUP.md
```

**Добавить все файлы определённого типа:**
```bash
git add *.md          # Все Markdown файлы
git add src/*.ts      # Все TypeScript файлы в папке src
```

### Шаг 3: Проверить, что добавлено

```bash
git status
```

Теперь файлы должны быть в разделе "Changes to be committed"

### Шаг 4: Зафиксировать изменения (commit)

**С коротким сообщением:**
```bash
git commit -m "Описание изменений"
```

**С подробным описанием:**
```bash
git commit -m "Краткое описание" -m "Подробное описание изменений"
```

**Примеры хороших сообщений коммитов:**
```bash
git commit -m "Fix database configuration for SQLite"
git commit -m "Add setup documentation in Russian"
git commit -m "Update Example model schema"
```

### Шаг 5: Отправить изменения на GitHub

**Отправить в текущую ветку:**
```bash
git push
```

**Отправить в конкретную ветку:**
```bash
git push origin copilot/debug-site-issues
```

**При первом push новой ветки:**
```bash
git push -u origin название-ветки
```

---

## Практические примеры

### Пример 1: Простое изменение одного файла

```bash
# 1. Изменили файл README.md в редакторе

# 2. Проверяем изменения
git status

# 3. Смотрим, что именно изменилось
git diff README.md

# 4. Добавляем файл
git add README.md

# 5. Коммитим
git commit -m "Update README with installation instructions"

# 6. Отправляем на GitHub
git push
```

### Пример 2: Множественные изменения

```bash
# 1. Изменили несколько файлов

# 2. Проверяем все изменения
git status

# 3. Просматриваем изменения
git diff

# 4. Добавляем все файлы
git add .

# 5. Коммитим с описанием
git commit -m "Fix site issues" -m "- Updated database schema
- Fixed mappers
- Added documentation"

# 6. Отправляем
git push
```

### Пример 3: Отменить изменения (до коммита)

```bash
# Отменить изменения в конкретном файле
git checkout -- filename.js

# Убрать файл из staging area (но сохранить изменения)
git reset HEAD filename.js

# Отменить все незакоммиченные изменения (ОСТОРОЖНО!)
git reset --hard HEAD
```

---

## Работа с ветками

### Создать новую ветку

```bash
# Создать и переключиться на новую ветку
git checkout -b feature/new-feature

# Или современный способ
git switch -c feature/new-feature
```

### Переключиться между ветками

```bash
# Переключиться на существующую ветку
git checkout main
# или
git switch main
```

### Посмотреть все ветки

```bash
# Локальные ветки
git branch

# Все ветки (включая удалённые)
git branch -a
```

### Объединить ветки (merge)

```bash
# Переключиться на главную ветку
git checkout main

# Объединить с другой веткой
git merge feature/new-feature
```

---

## Часто используемые команды

### Просмотр истории

```bash
# Показать историю коммитов
git log

# Краткая история (одна строка на коммит)
git log --oneline

# История с графом веток
git log --oneline --graph --all

# Последние 5 коммитов
git log -5
```

### Просмотр изменений

```bash
# Показать незакоммиченные изменения
git diff

# Показать изменения в конкретном файле
git diff filename.js

# Показать изменения, добавленные в staging
git diff --staged

# Показать изменения между ветками
git diff main..feature-branch
```

### Синхронизация с удалённым репозиторием

```bash
# Получить изменения с сервера (без применения)
git fetch

# Получить и применить изменения
git pull

# Получить изменения из конкретной ветки
git pull origin main
```

### Отмена коммитов

```bash
# Отменить последний коммит (но сохранить изменения)
git reset --soft HEAD~1

# Отменить последний коммит (удалить изменения) - ОСТОРОЖНО!
git reset --hard HEAD~1

# Создать новый коммит, отменяющий предыдущий
git revert HEAD
```

---

## Полезные советы

### 1. Часто проверяйте статус
```bash
git status
```

### 2. Используйте .gitignore
Создайте файл `.gitignore` для исключения ненужных файлов:
```
node_modules/
.env
*.log
dist/
.next/
```

### 3. Пишите понятные сообщения коммитов
❌ Плохо:
```bash
git commit -m "fix"
git commit -m "update"
```

✅ Хорошо:
```bash
git commit -m "Fix database connection timeout issue"
git commit -m "Update user authentication logic"
```

### 4. Коммитьте логически связанные изменения
Не делайте один большой коммит со всеми изменениями. Разбивайте на логические части:
```bash
git add database/schema.prisma
git commit -m "Update database schema for new fields"

git add lib/mappers.ts
git commit -m "Fix data transformation in mappers"
```

### 5. Проверяйте изменения перед коммитом
```bash
git diff           # Что изменилось
git status         # Статус файлов
git diff --staged  # Что будет закоммичено
```

---

## Текущий статус вашего репозитория

```bash
# Проверить статус
git status

# Посмотреть последние коммиты
git log --oneline -5

# Посмотреть, на какой ветке находитесь
git branch
```

Вы сейчас находитесь на ветке: `copilot/debug-site-issues`

Все изменения уже сохранены и отправлены на GitHub!

---

## Что делать дальше?

1. **Создать Pull Request на GitHub:**
   - Зайдите на https://github.com/xfssr/mss
   - Нажмите "Compare & pull request"
   - Опишите изменения
   - Создайте PR для слияния с основной веткой

2. **Продолжить работу над проектом:**
   ```bash
   # Внести новые изменения
   # Добавить файлы
   git add .
   # Закоммитить
   git commit -m "Описание новых изменений"
   # Отправить
   git push
   ```

3. **Вернуться к основной ветке:**
   ```bash
   git checkout main
   git pull
   ```

---

## Полезные ресурсы

- [Git Documentation (русский)](https://git-scm.com/book/ru/v2)
- [GitHub Guides](https://guides.github.com/)
- [Интерактивное обучение Git](https://learngitbranching.js.org/?locale=ru_RU)

---

## Быстрая шпаргалка

```bash
# Базовый цикл работы
git status                           # Проверить статус
git add .                            # Добавить все изменения
git commit -m "Описание изменений"   # Закоммитить
git push                             # Отправить на GitHub

# Просмотр
git log --oneline -10               # История коммитов
git diff                            # Посмотреть изменения
git branch                          # Список веток

# Работа с ветками
git checkout -b новая-ветка         # Создать новую ветку
git checkout main                   # Переключиться на main
git merge другая-ветка              # Слить ветки

# Синхронизация
git pull                            # Получить изменения с сервера
git push                            # Отправить изменения на сервер

# Отмена
git checkout -- файл                # Отменить изменения в файле
git reset HEAD файл                 # Убрать из staging
git reset --soft HEAD~1             # Отменить последний коммит
```

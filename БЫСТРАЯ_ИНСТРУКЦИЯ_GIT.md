# Как сохранить изменения в Git - Быстрая инструкция

## 🚀 Пять простых шагов

### Шаг 1️⃣: Проверьте, что изменилось
```bash
git status
```
Увидите список изменённых файлов

---

### Шаг 2️⃣: Добавьте файлы для сохранения
```bash
# Добавить все файлы
git add .

# ИЛИ добавить конкретный файл
git add имя-файла.js
```

---

### Шаг 3️⃣: Сохраните изменения (commit)
```bash
git commit -m "Описание ваших изменений"
```

**Примеры хороших описаний:**
- `"Добавил новую функцию оплаты"`
- `"Исправил ошибку в форме регистрации"`
- `"Обновил документацию"`

---

### Шаг 4️⃣: Отправьте на GitHub
```bash
git push
```

---

### Шаг 5️⃣: Проверьте результат
Зайдите на GitHub и убедитесь, что изменения появились!

---

## ✅ Полный пример

```bash
# 1. Вы изменили файл app.js

# 2. Проверяем изменения
git status

# 3. Добавляем файл
git add app.js

# 4. Сохраняем с описанием
git commit -m "Добавил валидацию email в форме"

# 5. Отправляем на GitHub
git push
```

---

## ⚡ Ещё быстрее - всё в одной команде

```bash
# Добавить все файлы и сразу закоммитить
git add . && git commit -m "Ваше описание" && git push
```

---

## 🔄 Если нужно отменить

### Отменить изменения в файле (до commit)
```bash
git checkout -- имя-файла.js
```

### Убрать файл из списка для commit
```bash
git reset HEAD имя-файла.js
```

---

## 📋 Текущий статус вашего проекта

Проверьте командой:
```bash
cd /home/runner/work/mss/mss
git status
```

**Результат:** `nothing to commit, working tree clean`

Это значит, что все изменения уже сохранены! ✅

---

## 📊 Что уже сделано в вашем репозитории

Последние изменения:
```bash
git log --oneline -5
```

Выведет:
```
2592457 Add Russian documentation explaining all fixes
69909ef Fix site issues: correct database mappers and add dev scripts
53c8228 Fix database configuration: migrate from PostgreSQL to SQLite
```

Все эти изменения уже в репозитории на GitHub!

---

## 🎯 Что делать дальше?

1. **Посмотреть изменения на GitHub:**
   - Откройте https://github.com/xfssr/mss
   - Выберите ветку `copilot/debug-site-issues`
   - Увидите все файлы и коммиты

2. **Создать Pull Request:**
   - На GitHub нажмите "Compare & pull request"
   - Опишите изменения
   - Создайте PR для объединения с основной веткой

3. **Продолжить разработку:**
   - Внесите новые изменения в файлы
   - Повторите шаги 1-4

---

## 💡 Полезные команды

```bash
# Посмотреть историю изменений
git log --oneline

# Посмотреть, что именно изменилось в файлах
git diff

# Посмотреть все ветки
git branch -a

# Переключиться на другую ветку
git checkout название-ветки
```

---

## 📚 Подробная документация

Для более детальной информации смотрите файл: **КАК_РАБОТАТЬ_С_GIT.md**

---

## ❓ Частые вопросы

**Q: Как узнать, сохранены ли мои изменения?**
```bash
git status  # Покажет "nothing to commit" если всё сохранено
```

**Q: Как посмотреть, что я изменил?**
```bash
git diff  # Покажет все незакоммиченные изменения
```

**Q: Забыл описание коммита, как изменить?**
```bash
git commit --amend -m "Новое описание"
```

**Q: Как отправить изменения в другую ветку?**
```bash
git push origin название-ветки
```

---

**✅ Всё готово! Ваши изменения уже сохранены и находятся на GitHub!**

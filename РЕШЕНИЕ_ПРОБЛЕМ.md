# Что пошло не так и как это было исправлено

## 🔄 Последние изменения: Переход на PostgreSQL (Neon) для Vercel

### Проблема
Проект был настроен на локальную разработку с SQLite, но вы хотите развернуть сайт на Vercel с базой данных на Neon (serverless PostgreSQL).

### Решение
Проект полностью переконфигурирован для работы с PostgreSQL и Neon:

1. **База данных**: Изменен провайдер с SQLite на PostgreSQL в `prisma/schema.prisma`
2. **Миграции**: Пересозданы все миграции для синтаксиса PostgreSQL
3. **Переменные окружения**: Обновлен `.env.example` с примером подключения к Neon
4. **Скрипты сборки**: Обновлен `vercel-build` для автоматического применения миграций
5. **Документация**: Полностью обновлены README.md и SETUP.md с инструкциями для Vercel + Neon

### Что нужно сделать

1. **Создайте базу данных на Neon**:
   - Зарегистрируйтесь на https://neon.tech
   - Создайте новый проект
   - Скопируйте строку подключения (Pooled connection)

2. **Настройте переменные в Vercel**:
   ```
   DATABASE_URL=postgresql://...@neon.tech/neondb?sslmode=require
   NEXT_PUBLIC_SITE_URL=https://your-site.vercel.app
   NEXT_PUBLIC_WHATSAPP_PHONE=972509656366
   ADMIN_PASSWORD=ваш-пароль
   ADMIN_COOKIE_SECRET=длинная-случайная-строка
   ```

3. **Разверните на Vercel**:
   - Подключите GitHub репозиторий
   - Нажмите Deploy
   - Vercel автоматически применит миграции и заполнит БД

Подробные инструкции смотрите в файлах README.md и SETUP.md.

---

## 🔴 Предыдущие проблемы (уже исправлены):

### 1. База данных PostgreSQL вместо SQLite (исправлено в первой версии)
**Что случилось**: В конфигурации Prisma (`schema.prisma`) была указана база данных PostgreSQL, но в проекте фактически использовалась SQLite база данных (`dev.db`).

**Почему сайт упал**: Prisma не мог подключиться к базе данных, так как пытался использовать PostgreSQL драйвер для SQLite файла.

**Как исправлено**: 
- Изменен тип базы данных в `schema.prisma` с `postgresql` на `sqlite`
- Обновлен `migration_lock.toml`
- Пересозданы миграции с правильным синтаксисом для SQLite

**Обновление**: Теперь проект снова использует PostgreSQL, но через Neon для Vercel deployment.

### 2. Несоответствие структуры базы данных и кода
**Что случилось**: В коде приложения использовались поля `mediaType`, `mediaUrl`, `posterUrl` для примеров работ, но в базе данных были старые поля `previewImage` и `videoUrl`.

**Почему сайт упал**: При запросе к базе данных Prisma пытался получить несуществующие колонки, что вызывало ошибку.

**Как исправлено**: 
- Обновлена модель `Example` в schema.prisma
- Создана новая миграция с правильной структурой таблицы
- Пересоздана база данных с новой структурой

### 3. Неправильное преобразование данных настроек
**Что случилось**: Функция `dbSettingsToUi` просто возвращала сырые данные из базы вместо преобразования их в нужный формат.

**Почему сайт упал**: В UI компонентах ожидались объекты формата `{ he: "текст", en: "text" }`, но приходили `undefined`, что вызывало ошибку "Cannot read properties of undefined".

**Как исправлено**: 
- Переписана функция `dbSettingsToUi` для правильного преобразования полей

### 4. Отсутствие зависимостей и окружения
**Что случилось**: 
- Не был установлен `node_modules` (npm пакеты)
- Отсутствовал файл `.env` с переменными окружения
- Не были сгенерированы клиенты Prisma

**Почему сайт упал**: Без зависимостей приложение вообще не может запуститься.

**Как исправлено**: 
- Создан файл `.env` с правильной конфигурацией
- Установлены все зависимости через `npm install`
- Сгенерирован Prisma Client

### 5. Отсутствие dev скриптов
**Что случилось**: В `package.json` не было скриптов `dev` и `start` для запуска приложения.

**Как исправлено**: Добавлены необходимые npm скрипты.

## ✅ Итог

Сайт теперь полностью готов для развертывания на Vercel с Neon PostgreSQL:
- ✅ База данных настроена на PostgreSQL (Neon)
- ✅ Миграции переписаны для PostgreSQL синтаксиса
- ✅ Автоматическое применение миграций при deploy
- ✅ Документация обновлена с инструкциями для Vercel
- ✅ Структура данных соответствует коду
- ✅ Все данные преобразуются правильно

## 🚀 Как развернуть:

См. подробные инструкции в README.md и SETUP.md

Кратко:
1. Создайте БД на https://neon.tech
2. Настройте переменные окружения в Vercel
3. Нажмите Deploy в Vercel

## 📝 Локальная разработка (опционально)

Если хотите разрабатывать локально:

```bash
# Установить зависимости
npm install

# Создать .env с DATABASE_URL от Neon
cp .env.example .env
# Отредактируйте .env и добавьте свой DATABASE_URL

# Применить миграции
npx prisma migrate deploy

# Заполнить БД начальными данными
npx prisma db seed

# Запустить dev сервер
npm run dev
```
